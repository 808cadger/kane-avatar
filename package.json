{
  "name": "kane-avatar",
  "version": "0.1.0",
  "description": "Kane — a life-like, proactive 3D AI companion avatar, embeddable in any app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:embed": "esbuild src/embed.js --bundle --format=iife --outfile=dist/kane.js --minify"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@pixiv/three-vrm": "^3.5.4",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "playwright": "^1.61.1",
    "vite": "^8.1.3"
  }
}
